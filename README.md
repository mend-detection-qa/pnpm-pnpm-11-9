# pnpm 11.9 — Mend-SCA Probe

Auto-generated by the Mend-SCA pipeline on 2026-06-23.

## Categories

| Category | What is tested |
|---|---|
| `lockfile_format` | All `packages` entries carry `integrity: sha512-…` hashes (new in pnpm 11.9 for on-demand registries). Mend must map these into `hashes[]` on each package node. |
| `checksum_signing` | `.npmrc` sets `verify-store-integrity=true`, activating pnpm 11.9's stricter `ERR_PNPM_MISSING_TARBALL_INTEGRITY` enforcement. |
| `tree_structure` | Snapshot keys use deterministic peer-suffixed forms e.g. `react-dom@18.3.1(react@18.3.1)`. `PnpmParserV9Impl` must strip the parenthesised suffix to recover bare version `18.3.1`. Failure produces malformed version values in the dependency tree. |

## Dependency tree (expected)

- `react@18.3.1` (direct)
- `react-dom@18.3.1` (direct) → `scheduler@0.23.2`
- `@testing-library/react@14.2.1` (dev) → `@testing-library/dom@9.3.4`
- `loose-envify@1.4.0` (transitive) → `js-tokens@4.0.0`

Total: **7 packages**

## Mend failure modes

- `PnpmParserV9Impl` strips peer suffix incorrectly → version field becomes `18.3.1(react@18.3.1)` instead of `18.3.1`.
- Scanner skips `packages` section integrity → `hashes[]` empty for on-demand-registry packages.
- `verify-store-integrity` mismatch causes install failure before scan (environment issue).

## Release notes

https://github.com/pnpm/pnpm/releases/tag/v11.9.0
